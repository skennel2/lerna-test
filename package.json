{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "start": "lerna run --scope @mee/main-app start --parallel",
    "watch": "lerna run watch --stream",
    "sss": "npm run watch && npm run start",
    "bootstrap": "lerna bootstrap"
  }
}
